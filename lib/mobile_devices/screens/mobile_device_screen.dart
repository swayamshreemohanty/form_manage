import 'package:flutter/material.dart';

class MobileDeviceScreen extends StatelessWidget {
  const MobileDeviceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
